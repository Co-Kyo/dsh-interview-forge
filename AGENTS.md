# dsh-interview-forge Agent Contract

> 状态：active
> 定位：根级 agent 契约，只放跨会话不变量和分级规则。流程细节、架构提案与审计记录
> 在维护者私有工作档案（独立仓库，不随本仓库发布）。

## 1. 项目边界

- InterviewForge 是 DeepSeek Harness 的快速练习插件：出题 → ⚡ 浮层答题 → 归因分析 → 雷达报告。
- 形态：host 工具 + 浏览器浮层 UI + 配套 skill，单包发布，随 `dsh` 启动加载、重启不丢。
- 不引入云端服务、账号体系、遥测上报；练习档案（quiz/result/report）只落本地磁盘。
- 公开面（README、docs/、提交信息）不得出现内部迭代痕迹：版本翻新文件、会话记录、审计过程。

## 2. 冷启动

1. 读本文档确认边界与执行分级。
2. 本仓库只保留已锁定结论：`docs/` 下为脱敏后的架构说明与排障指南。
3. 实现契约、架构提案、多轮审计、决策记录、HANDOVER 均在维护者私有工作档案；
   需要时向维护者索取。
4. 未完成任务以 HANDOVER 为准，不默认跨会话延续。

## 3. 执行分级

| 级别 | 适用 | 最低要求 |
|---|---|---|
| T0 | 只读分析、问答、路径修正、明显 typo | 直接执行 |
| T1 | 标准代码、文档、构建或测试变更 | 会话目录写 `PLAN.md` 与 `WORKLOG.md`（在私有工作区） |
| T2 | 多文件重构、并行任务、高风险变更、subagent 委托 | T1 基础上加 `GATES.md`、任务卡、独立 worktree、显式 review |

会话目录按需创建，不为空文件而空文件。

## 4. 通用规则

- 先确认目标和边界；有歧义且高风险时先问，低风险时给出合理假设并记录。
- 不扩大用户请求范围；发现相关问题先记录，不擅自顺手改。
- 变更前选择最小充分验证：build、e2e 或人工冒烟。
- T1/T2 必须把实际命令、输出摘要、改动文件写入 WORKLOG；不接受「应该完成了」。
- 理解错误时写 adjustment 并重建任务，不在错误结果上打补丁。
- 并行任务必须拆清依赖和写集；写集重叠时串行。T2 的 reviewer 必须给出显式 PASS/FAIL。

### 推送门禁（Release Gate）

每次推送 origin main 前：

1. `npm run build` 通过。
2. `node scripts/render-report.e2e.cjs` 通过（报告统计契约回归）。
3. `npm run e2e` 通过——需 live 宿主运行时；宿主不可用时在 PR/交接中注明并跳过该项，其余照常。
4. `sh scripts/check-public-boundary` 通过。
5. 用户可见行为变化时同步 package.json version 与 README 徽章。

沙箱内无法完成某项验证时不推送，等用户提供宿主机结果。

## 5. 用户门

到达以下情况必须停下确认：

- 改变产品范围、存储模型或发布策略。
- 引入新的运行时依赖。
- 执行删除、覆盖、发布等不可逆操作。
- 自动验证通过但用户可见行为需要产品验收。
- 需要改变本文件或协作协议本身。

## 6. 文档关系

| 位置 | 性质 |
|---|---|
| 本文件 | 跨会话不变量（公开） |
| `docs/*`（本仓库） | 已锁定结论的脱敏版（公开） |
| 维护者私有工作档案 | 提案、审计、决策记录、协议细节、HANDOVER（私有仓） |
| 会话目录 | 临时工作记忆，不入任何远端，确认后销毁 |